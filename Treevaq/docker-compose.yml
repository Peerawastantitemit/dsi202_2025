version: '3.8'

services:
  web:
    build: .
    container_name: Treevaq
    volumes:
      - .:/usr/src/app
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - DJANGO_SETTINGS_MODULE=myproject.settings  # เพิ่มเพื่อชี้ settings
    tty: true
